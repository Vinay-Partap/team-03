import {useState,useEffect} from "react"; import {useDispatch,useSelector} from "react-redux"; import {useNavigate,Link} from "react-router-dom"; import {loginUser,clearError} from "../../redux/slices/authSlice"; import {toast,Toaster} from "react-hot-toast"; import {Mail,Lock,Eye,EyeOff,ArrowRight} from "lucide-react"; import {motion} from "framer-motion"; import Auth0LoginButton from "../../components/auth/Auth0LoginButton";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[show,setShow]=useState(false);const d=useDispatch(),n=useNavigate();const{loading,error,token,user}=useSelector(s=>s.auth);useEffect(()=>{d(clearError())},[d]);useEffect(()=>{if(token&&user)n(user.role==="admin"?"/admin/dashboard":user.role==="official"?"/government/dashboard":"/dashboard")},[token,user,n]);useEffect(()=>{if(error){toast.error(error);d(clearError())}},[error,d]);const submit=e=>{e.preventDefault();d(loginUser({email,password}))};return <motion.div initial={{opacity:0,y:16}} animate={{opacity:1,y:0}}><Toaster/><p className="text-sm font-semibold text-blue-600">WELCOME BACK</p><h1 className="mt-2 text-2xl sm:text-3xl font-bold tracking-tight text-slate-950">Sign in to GovIntel</h1><p className="mt-2 text-sm text-slate-500">Access policies, schemes, and personalised insights.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="relative block"><Mail className="absolute left-4 top-4 h-5 w-5 text-slate-400"/><input aria-label="Email address" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="h-14 w-full rounded-xl border border-slate-200 bg-white pl-12 pr-4 text-sm outline-none transition focus:border-blue-500 focus:ring-4 focus:ring-blue-100"/></label><label className="relative block"><Lock className="absolute left-4 top-4 h-5 w-5 text-slate-400"/><input aria-label="Password" type={show?"text":"password"} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="h-14 w-full rounded-xl border border-slate-200 bg-white pl-12 pr-12 text-sm outline-none transition focus:border-blue-500 focus:ring-4 focus:ring-blue-100"/><button aria-label="Toggle password visibility" type="button" onClick={()=>setShow(!show)} className="absolute right-4 top-4 text-slate-400">{show?<EyeOff/>:<Eye/>}</button></label><div className="flex justify-between text-sm"><label className="flex gap-2 text-slate-600"><input type="checkbox"/> Remember me</label><Link to="/forgot-password" className="font-semibold text-blue-600">Forgot password?</Link></div><motion.button whileTap={{scale:.98}} disabled={loading} className="h-14 w-full rounded-xl bg-gradient-to-r from-blue-600 to-blue-700 font-semibold text-white shadow-lg shadow-blue-200">{loading?"Signing in…":"Sign in"} <ArrowRight className="ml-1 inline h-4"/></motion.button></form><div className="my-5 flex items-center gap-3 text-xs text-slate-400 before:h-px before:flex-1 before:bg-slate-200 after:h-px after:flex-1 after:bg-slate-200">OR</div><Auth0LoginButton/><p className="mt-4 sm:mt-5 text-center text-sm text-slate-600">New to GovIntel? <Link className="font-semibold text-blue-600" to="/register">Create an account</Link></p></motion.div>}
