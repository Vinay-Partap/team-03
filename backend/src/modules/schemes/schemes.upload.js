const multer=require('multer');const path=require('path');const crypto=require('crypto');const allowed=new Set(['application/pdf','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document']);const storage=multer.diskStorage({destination:path.join(process.cwd(),'uploads','schemes'),filename:(_r,f,cb)=>cb(null,`${crypto.randomUUID()}${path.extname(f.originalname).toLowerCase()}`)});module.exports=multer({storage,limits:{fileSize:10*1024*1024},fileFilter:(_r,f,cb)=>allowed.has(f.mimetype)?cb(null,true):cb(new Error('Only PDF, DOC, and DOCX documents are allowed'))});
