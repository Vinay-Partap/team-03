# Development requests to /api are proxied by Vite to this backend.
VITE_PROXY_TARGET=http://localhost:5000
# Leave this as /api for a same-origin production reverse proxy. Set an HTTPS API URL only when deployed separately.
VITE_API_URL=/api

# Auth0 SPA configuration (safe to expose; do not put a client secret in Vite variables)
VITE_AUTH0_DOMAIN=dev-q2hbezioi4vl4y0g.us.auth0.com
VITE_AUTH0_CLIENT_ID=IWrcVLB9kArIpSheWIBTGvcCFriYfrkR
